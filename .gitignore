# ---------------------------------------------------------------- 构建产物
/entry/build/
/entry/.preview/
/build/
/.hvigor/
/.appanalyzer/
/oh_modules/
/node_modules/
oh-package-lock.json5

# ---------------------------------------------------------------- 签名材料
# 调试/正式签名都会把证书、私钥、Profile 写进 build-profile.json5。
# 这份文件本身要提交（否则别人 clone 下来构建不了），但材料文件绝不能进库。
*.p12
*.cer
*.p7b
*.csr
/entry/.signing/

# ---------------------------------------------------------------- IDE
/.idea/
/.vscode/
*.iml
/local.properties
.cxx/
/.clangd
/.clang-format
/.clang-tidy

# ---------------------------------------------------------------- 本地调试产物
# shots/ 是走查时的截图与控件树 dump：里面有本机内网地址、访问令牌、
# 以及会话列表里真实的好友名字，一律不进库。
/shots/
/.workbuddy/

# 逐轮改代码时留下的「整段替换」补丁脚本与回滚备份。
# 它们带着当时的调试数据（真实好友名等），且本身就是一次性的，
# 对后来的人没有价值 —— 真正要看的是它们改完之后的 Index.ets。
/tools/patch_*.py
/tools/.device
*.bak
*.bak2
*.bak3

# ---------------------------------------------------------------- 系统
.DS_Store
Thumbs.db
desktop.ini
